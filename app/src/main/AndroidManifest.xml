<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zghoststudios.aninterface">

    <application
        android:allowBackup="true"
        android:icon="@drawable/walkman"
        android:label="@string/app_name"
        android:roundIcon="@drawable/walkman"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ectomorfo"></activity>
        <activity android:name=".Mesomorfo" />
        <activity android:name=".Endormorfo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>